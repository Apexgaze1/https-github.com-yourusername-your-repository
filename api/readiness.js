const required=["SQUARE_ACCESS_TOKEN","SQUARE_LOCATION_ID","SQUARE_WEBHOOK_SIGNATURE_KEY","SQUARE_WEBHOOK_NOTIFICATION_URL","CJ_ACCESS_TOKEN"];export default function handler(req,res){const checks=Object.fromEntries(required.map(k=>[k,Boolean(process.env[k])]));const ready=Object.values(checks).every(Boolean);res.status(ready?200:503).json({ready,checks,platform:"vercel",timestamp:new Date().toISOString()})}